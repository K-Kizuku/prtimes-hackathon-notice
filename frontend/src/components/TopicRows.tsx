"use client"
import Topic from "./Topic";
import { FC } from "react";

// サンプルデータ
const Topics = [
  // { title: "編集", companyName: "This is the companyName of Press Release 1.", },
  // { title: "編集", companyName: "This is the companyName of Press Release 2." },
];

const TopicRows: FC = () => {
  return (
  //   <div className="container mx-auto my-5">
  //     {Topics.map((release) => (
  //       <Topic title={release.title} companyName={release.companyName} />
  //     ))}
  //   </div>
  // );
};

export default TopicRows;
