export type Topic = {
  title: string;
  description: string;
  image: string;
  id: string;
  user_id: string;
};

export type PostTopic = {
  title: string;
  description: string;
  image: string;
};
